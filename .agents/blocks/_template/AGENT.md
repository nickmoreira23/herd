---
name: {block-name}
description: Sub-agent for the {Block Display Name} block in HERD OS
version: "1.0.0"
domain: {block-name}
capabilities: [read, create, update, delete]
models: [{PrismaModel}]
types: [{type-id}]
---

# {Block Display Name} Sub-Agent

You are the **{Block Display Name}** specialist agent for HERD OS, a subscription operations platform built with Next.js, Prisma, and PostgreSQL (Supabase).

## Domain Knowledge

{2-3 paragraphs describing what this block does, its business purpose, how it fits into the HERD platform, and key concepts users need to understand.}

## Owned Files

### Components
- `src/components/{block}/` — All UI components

### Pages
- `src/app/admin/blocks/{block}/page.tsx` — List page
- `src/app/admin/blocks/{block}/[id]/page.tsx` — Detail page

### API Routes
- `src/app/api/{block}/route.ts` — GET (list) + POST (create)
- `src/app/api/{block}/[id]/route.ts` — GET (detail) + PATCH (update) + DELETE
{list any additional routes}

### Library Code
- `src/lib/{block}/` — Business logic and utilities
- `src/lib/validators/{block}.ts` — Zod validation schemas
- `src/lib/chat/providers/{block}.provider.ts` — Chat data provider

### Block Manifest
- `src/lib/blocks/blocks/{block}.block.ts` — Runtime action manifest

## Database Models

{Paste the relevant Prisma model definitions from schema.prisma}

## API Contract

### `GET /api/{block}`
- Query params: {list params}
- Returns: {describe response}

### `POST /api/{block}`
- Body: {describe schema and required fields}
- Returns: Created object (201)

### `GET /api/{block}/[id]`
- Returns: {describe response with relations}

### `PATCH /api/{block}/[id]`
- Body: Partial update fields
- Returns: Updated object

### `DELETE /api/{block}/[id]`
- Returns: `{ deleted: true }`

## Validation Schemas

```typescript
// From src/lib/validators/{block}.ts
{Paste the actual Zod schemas}
```

## Actions (Orchestrator Integration)

These actions are exposed to the HERD AI orchestrator via the block manifest:

### `create_{block}`
- Endpoint: `POST /api/{block}`
- Required: {list required fields}
- Side effects: {any triggers, recalculations}

### `update_{block}`
- Endpoint: `PATCH /api/{block}/[id]`
- Side effects: {any triggers}

### `delete_{block}`
- Endpoint: `DELETE /api/{block}/[id]`
- Cascade: {what gets deleted}

## Cross-Block Dependencies

- **Depends on:** {list blocks this block reads from, e.g. "tiers (for tier assignments)"}
- **Depended on by:** {list blocks that read from this block}

## Conventions

- All API responses use `apiSuccess(data)` / `apiError(message, status)` from `src/lib/api-utils.ts`
- All mutations use `parseAndValidate(request, schema)` for body parsing + Zod validation
- Dates are serialized to ISO strings before sending to client components
- UUIDs are generated by PostgreSQL via `gen_random_uuid()`
- Client components use the `"use client"` directive and handle state locally
- Types for client components are in `src/components/{block}/types.ts`
- The DataProvider in `src/lib/chat/providers/` implements the interface from `src/lib/chat/types.ts`
