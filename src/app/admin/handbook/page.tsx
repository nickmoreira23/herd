export default function HandbookPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-foreground mb-2">Handbook</h1>
      <p className="text-muted-foreground">
        Em construção — Sub-etapa 2 implementa o reader. Use o painel lateral
        para navegar.
      </p>
    </div>
  );
}
